- Criacao do CRUD, até o momento cadastro do usuario esta OK, estrutura da table
-> CREATE TABLE usario (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        nome VARCHAR(30),
        endereco,
        telefone
);
